/**
 * Standardized chart color palette for all Recharts visualizations.
 * Import from here instead of defining colors inline.
 */
export { chartPalette } from './brandTokens';

export { brandColors, semanticColors } from './brandTokens';

// Cartesian grid stroke color
export const gridStroke = '#E4E4E7';

// Tooltip style
export const tooltipStyle = {
  contentStyle: {
    borderRadius: 8,
    border: '1px solid #E4E4E7',
    boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
  },
};
